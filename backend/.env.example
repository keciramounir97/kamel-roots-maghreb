# Server
NODE_ENV=production
PORT=5000

# Database (preferred)
DATABASE_URL=mysql://USER:PASSWORD@HOST:3306/DBNAME

# Database (alternative components; will auto-build DATABASE_URL)
DB_HOST=
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=
SESSION_TTL_SECONDS=
RESET_CODE_TTL=

# CORS (optional; comma-separated)
# CORS_ORIGIN=https://rootsmaghreb.com,https://www.rootsmaghreb.com

# Uploads / frontend serving (optional)
FRONTEND_DIST_DIR=

# Email (optional)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Operational toggles (optional)
SKIP_ACTIVITY_LOGS=false
SKIP_MAINTENANCE=false
DEBUG_PRISMA=false
